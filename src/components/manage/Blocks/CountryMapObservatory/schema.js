const FilterSchema = (data) => {
  return {
    title: 'Country Map',
    fieldsets: [
      {
        id: 'default',
        title: 'Default',
        fields: [],
      },
    ],
    properties: {},
    required: [],
  };
};

export default FilterSchema;
